# portfolio-site 作品集网站 · 新电脑（Mac）使用说明

> 这是你的秋招个人作品集网站的**完整源码包**。GitHub 上已有备份，本文说明如何在新电脑上**下载、打开、修改、发布**。

---

## 一、这是什么

- **portfolio-site**：你的个人作品集网站（Vite + Vue3 + TailwindCSS，纯静态）
- 已托管 GitHub：https://github.com/lianghaoran1118-png/portfolio-site
- 线上网站：https://lianghaoran1118-png.github.io/portfolio-site/ （GitHub Actions 自动部署，改完 push 自动更新）
- 内含中英文切换、项目作品集、技能栈、关于我、中文/英文简历

---

## 二、两种获取方式（推荐方式 A）

### 方式 A：从 GitHub 下载最新代码（推荐 ✅）

**只要联网，用这种方式**，能拿到最新版本、且后续可直接同步修改：

```bash
git clone https://github.com/lianghaoran1118-png/portfolio-site.git
cd portfolio-site
```

> 本 ZIP 里的代码只是某个时间点的快照，GitHub 上才是最新/权威的版本。

### 方式 B：直接用本 ZIP 里的代码

解压后直接得到一个 `portfolio-site` 文件夹，跳过 clone。适合网络不便时用。
（若以后想同步回 GitHub，需在文件夹里执行 `git init` 并关联远程仓库，操作见文末「附」）

---

## 三、首次运行（Mac，只需一次）

### 1. 装好基础工具
```bash
xcode-select --install      # Git
brew install node           # Node.js（网站需要）
```

### 2. 安装依赖并启动
```bash
cd portfolio-site           # 进入项目目录
npm install                 # 安装依赖（node_modules 不入库，每台电脑首次都要装）
npm run dev                 # 启动本地预览
```
浏览器打开 **http://localhost:5173/** 即可看到网站（含中英文切换）。

---

## 四、用 CodeBuddy 打开和修改

1. 打开 **CodeBuddy**
2. 「打开文件夹」→ 选择 `portfolio-site` 目录
3. 直接对话提需求（改文字、改图片、加功能、修问题），CodeBuddy 帮你改代码
4. 想本地预览，让 CodeBuddy 运行 `npm run dev`

### 常用修改点

| 想改什么 | 改哪里 |
| --- | --- |
| 中文文字 / 个人信息 / 项目 | `src/data/content.js` |
| 英文文字（英文版） | `src/data/en.js` |
| 新增项目/技能/经历 | 两个文件对应数组里加对象 |
| 项目截图 | 放到 `public/images/`，在 `content.js` 里把 `imageSrc` 改为 `images/xxx.png` |
| 中文简历 PDF | 覆盖 `public/resume/resume.pdf` |
| 英文简历 PDF | 覆盖 `public/resume/resume_en.pdf` |
| 页面布局/样式 | `src/views/*.vue`、`src/components/*.vue` |

> ⚠️ **重要**：加新内容时 **中文 `content.js` 和英文 `en.js` 都要同步改**，否则切换英文会缺内容。

---

## 五、改完发布到线上（GitHub 自动部署）

在项目目录执行三条命令：

```bash
git add -A
git commit -m "描述本次修改"
git push
```

push 后 **GitHub Actions 自动重新构建部署**，约 1 分钟线上生效（https://lianghaoran1118-png.github.io/portfolio-site/）。

> 首次 push 会要求登录 GitHub：用户名填 `lianghaoran1118-png`，密码处填 **Personal Access Token**（GitHub → Settings → Developer settings → Personal access tokens → 生成，勾选 `repo` 权限）。

---

## 六、日常流程速记

```
改完代码 → git add -A → git commit -m "msg" → git push → 1 分钟后线上更新
```

常用命令：

| 操作 | 命令 |
| --- | --- |
| 从 GitHub 拉最新 | `git pull` |
| 本地预览 | `npm run dev` |
| 查看改动 | `git status` |
| 提交 | `git add -A && git commit -m "msg"` |
| 发布 | `git push` |

---

## 附：用 ZIP 方式时，想关联回 GitHub（可选）

如果走了「方式 B」但以后想 push 同步：

```bash
cd portfolio-site
git init -b main
git add -A
git commit -m "init"
git remote add origin https://github.com/lianghaoran1118-png/portfolio-site.git
git push -u origin main
```
（若远程已有内容，先 `git pull origin main --allow-unrelated-histories` 再 push）
